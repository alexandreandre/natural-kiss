// Stub pour Vitest : neutralise `import "server-only"` hors runtime React Server.
// Les modules serveur (ex. services d'accès aux données) restent testables en
// intégration sans dépendre du bundler Next.
export {};
